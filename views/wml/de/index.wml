<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE wml PUBLIC "-//WAPFORUM//DTD WML 1.1//EN"
    "http://www.wapforum.org/DTD/wml_1.1.xml">
<wml>
    <card id="chats" title="SuperBasic IM">
        <p>
            Seite {{page}} von {{totalPages}}
        </p>

        <p>
            {{#each chats}}
                <a href="/chats/{{this.id}}">
                    {{#if this.hasUnread}}
                        * {{this.name}}
                    {{else}}
                        {{this.name}}
                    {{/if}}
                </a><br/>
            {{else}}
                Keine Chats gefunden.
            {{/each}}
        </p>

        {{#if hasPreviousPage}}
            <p>
                <a href="{{listPath}}?page={{previousPage}}">Vorherige Seite</a>
            </p>
        {{/if}}

        {{#if hasNextPage}}
            <p>
                <a href="{{listPath}}?page={{nextPage}}">Nächste Seite</a>
            </p>
        {{/if}}

        <p>
            <a href="/">Startseite</a>
        </p>
    </card>
</wml>
